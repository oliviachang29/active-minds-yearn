---
label: Carousel
hide_body: true
fields:
- name: template
  type: text
  config:
    required: false
  label: template
  default: carousel
  hidden: true
- name: id
  type: include
  config: {}
  template: id
  label: id
- name: carousel_items
  type: field_group_list
  fields:
  - name: item
    type: include
    config: {}
    template: centered-text-w-bg-image
    label: Carousel Item - using centered-text-w-bg-image
  config:
    min: 
    max: 
    labelField: title
  label: Carousel Items
  description: The rotating slideshow of images and text (as seen in the home page!).
    You need to add at least one carousel item for it to work.
